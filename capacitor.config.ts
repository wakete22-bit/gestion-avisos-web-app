import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.adrianpozoest.gestionavisos',
  appName: 'gestionAvisos',
  webDir: 'www'
};

export default config;
